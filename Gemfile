source 'https://rubygems.org'

ruby '>= 3.3.0'

# Minimal dependencies: Ruby stdlib covers JSON, FileUtils, Time
# Optional gem for macOS notifications
gem 'terminal-notifier', '~> 2.0', require: false
gem 'rake', '~> 13.0'

group :development do
  gem 'test-unit', '~> 3.6'
  gem 'simplecov', '~> 0.22', require: false
end
